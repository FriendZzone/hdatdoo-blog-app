import React from 'react';
import {Typography} from '@mui/material'

function Header(props) {
    return (
        <Typography variant="h4" align="center" className='bl-header'>
           Blog
        </Typography>
    );
}

export default Header;